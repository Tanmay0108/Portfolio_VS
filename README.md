# Tanmay Portfolio

A static Next.js portfolio for Tanmay Potnurwar.

## Run locally

```powershell
npm install
npm run dev
```

The project content lives in `data/projects.ts`; every project page is statically generated at `/projects/[slug]`. The resume download is served from `public/resume/tanmay-resume.pdf`.
